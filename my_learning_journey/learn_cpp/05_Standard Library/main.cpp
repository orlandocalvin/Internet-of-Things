#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    double result = floor(3.7);
    cout << "The floor of 3.7 is: " << result << endl;

    double power = pow(2, 3);
    cout << "2 squared by 3 is: " << power << endl;
    return 0;
}